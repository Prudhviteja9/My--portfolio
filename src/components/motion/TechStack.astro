---
// Tech stack icon display with hover animations
interface Tech {
	name: string;
	icon: string;
}

interface Props {
	techs?: Tech[];
}

const techs = [
	{ name: "Python", icon: "🐍" },
	{ name: "PyTorch", icon: "🔥" },
	{ name: "OpenCV", icon: "👁️" },
	{ name: "FastAPI", icon: "⚡" },
	{ name: "Docker", icon: "🐳" },
	{ name: "ML", icon: "🧠" },
	{ name: "Data", icon: "📊" },
	{ name: "AI", icon: "🤖" },
];
---

<div class="tech-stack-container">
	<div class="tech-stack-scroller">
		{
			techs.map((tech) => (
				<div class="tech-item" title={tech.name}>
					<span class="tech-icon">{tech.icon}</span>
					<span class="tech-label">{tech.name}</span>
				</div>
			))
		}
		{
			techs.map((tech) => (
				<div class="tech-item" title={tech.name}>
					<span class="tech-icon">{tech.icon}</span>
					<span class="tech-label">{tech.name}</span>
				</div>
			))
		}
	</div>
</div>

<style>
	.tech-stack-container {
		width: 100%;
		overflow: hidden;
		margin: 2rem 0;
		padding: 2rem 0;
		position: relative;
	}

	.tech-stack-container::before,
	.tech-stack-container::after {
		content: "";
		position: absolute;
		top: 0;
		width: 100px;
		height: 100%;
		pointer-events: none;
		z-index: 10;
	}

	.tech-stack-container::before {
		left: 0;
		background: linear-gradient(90deg, rgb(26, 26, 26) 0%, transparent 100%);
	}

	.tech-stack-container::after {
		right: 0;
		background: linear-gradient(270deg, rgb(26, 26, 26) 0%, transparent 100%);
	}

	.tech-stack-scroller {
		display: flex;
		gap: 2rem;
		animation: scroll 30s linear infinite;
		padding: 0 2rem;
	}

	.tech-stack-scroller:hover {
		animation-play-state: paused;
	}

	.tech-item {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 0.5rem;
		padding: 1rem;
		border-radius: 12px;
		background: linear-gradient(135deg, rgba(72, 255, 255, 0.1), rgba(22, 255, 0, 0.1));
		border: 1px solid rgba(72, 255, 255, 0.2);
		white-space: nowrap;
		transition: all 0.3s ease;
		cursor: pointer;
		flex-shrink: 0;
		min-width: fit-content;
	}

	.tech-item:hover {
		background: linear-gradient(135deg, rgba(72, 255, 255, 0.2), rgba(22, 255, 0, 0.2));
		border-color: rgba(72, 255, 255, 0.5);
		transform: translateY(-8px) scale(1.05);
		box-shadow: 0 10px 30px rgba(72, 255, 255, 0.3);
	}

	.tech-icon {
		font-size: 1.75rem;
		transition: transform 0.3s ease;
	}

	.tech-item:hover .tech-icon {
		transform: scale(1.2) rotate(10deg);
	}

	.tech-label {
		font-size: 0.875rem;
		font-weight: 600;
		color: #ffffff;
		letter-spacing: 0.5px;
	}

	@keyframes scroll {
		0% {
			transform: translateX(0);
		}
		100% {
			transform: translateX(-50%);
		}
	}

	@media (max-width: 768px) {
		.tech-stack-container {
			margin: 1rem 0;
			padding: 1rem 0;
		}

		.tech-item {
			padding: 0.75rem;
			gap: 0.3rem;
		}

		.tech-icon {
			font-size: 1.25rem;
		}

		.tech-label {
			font-size: 0.75rem;
		}
	}
</style>
